---
- name: Unit Test for Tool Image
  hosts: "{{ test_subject }}"
  gather_facts: true
  become: true
  tasks:
  - name:
    stat:
      path: /opt/bro/bin/bro
    register: bro_output
    
  - name: write jstack                                                                                
    copy:                                                                                              
      content: "{{ bro_output.stdout }}" 
      dest: /tmp/bro_output.txt
